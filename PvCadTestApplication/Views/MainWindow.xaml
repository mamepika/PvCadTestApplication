<Window x:Class="PvCadTestApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:l="clr-namespace:PvCadTestApplication.ViewModels"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <l:MainWindowViewModel x:Key="MainWindowViewModel"></l:MainWindowViewModel>
    </Window.Resources>
    <Grid>
        <Button Content="Button" HorizontalAlignment="Left" Margin="413,269,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click"/>
        <DataGrid HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Height="222" Width="497" ItemsSource="{Binding}"/>

    </Grid>
</Window>
